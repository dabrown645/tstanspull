- hosts: mygroup
  tasks:
    - debug:
        msg: "I'm in group 'mygroup'"

- hosts: onlyfoo
  tasks:
    - debug:
        msg: "I'm in group 'onlyfoo'"

- hosts: onlybar
  tasks:
    - debug:
        msg: "I'm in group 'onlybar'"

- hosts: popos
  tasks:
    - debug:
        msg: "I'm in group 'popos'"

          #- name: configure ansible-pull
          #  hosts: all
          #  roles:
          #    - role: ansible-pull
          #      ansible_pull_url: https://git.example.com/ansible.git
